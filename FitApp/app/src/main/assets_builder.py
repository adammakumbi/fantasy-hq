# This script builds the merged HTML from the two source files
# We'll create it directly instead

merged = open("/home/claude/FitApp/app/src/main/merged_app.html", "w")
merged.write("placeholder")
merged.close()
print("ok")
